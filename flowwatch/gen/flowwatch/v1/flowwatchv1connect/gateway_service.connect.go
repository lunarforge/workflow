// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: flowwatch/v1/gateway_service.proto

package flowwatchv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/lunarforge/workflow/flowwatch/gen/flowwatch/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GatewayServiceName is the fully-qualified name of the GatewayService service.
	GatewayServiceName = "flowwatch.v1.GatewayService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GatewayServiceRegisterProcedure is the fully-qualified name of the GatewayService's Register RPC.
	GatewayServiceRegisterProcedure = "/flowwatch.v1.GatewayService/Register"
)

// GatewayServiceClient is a client for the flowwatch.v1.GatewayService service.
type GatewayServiceClient interface {
	// Register opens a persistent bidirectional stream between a subsystem and
	// the gateway. The subsystem sends a RegisterRequest as its first message,
	// then heartbeat pongs. The gateway sends a RegisterResponse followed by
	// heartbeat pings and optional commands.
	Register(context.Context) *connect.BidiStreamForClient[v1.SubsystemMessage, v1.GatewayMessage]
}

// NewGatewayServiceClient constructs a client for the flowwatch.v1.GatewayService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGatewayServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GatewayServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gatewayServiceMethods := v1.File_flowwatch_v1_gateway_service_proto.Services().ByName("GatewayService").Methods()
	return &gatewayServiceClient{
		register: connect.NewClient[v1.SubsystemMessage, v1.GatewayMessage](
			httpClient,
			baseURL+GatewayServiceRegisterProcedure,
			connect.WithSchema(gatewayServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gatewayServiceClient implements GatewayServiceClient.
type gatewayServiceClient struct {
	register *connect.Client[v1.SubsystemMessage, v1.GatewayMessage]
}

// Register calls flowwatch.v1.GatewayService.Register.
func (c *gatewayServiceClient) Register(ctx context.Context) *connect.BidiStreamForClient[v1.SubsystemMessage, v1.GatewayMessage] {
	return c.register.CallBidiStream(ctx)
}

// GatewayServiceHandler is an implementation of the flowwatch.v1.GatewayService service.
type GatewayServiceHandler interface {
	// Register opens a persistent bidirectional stream between a subsystem and
	// the gateway. The subsystem sends a RegisterRequest as its first message,
	// then heartbeat pongs. The gateway sends a RegisterResponse followed by
	// heartbeat pings and optional commands.
	Register(context.Context, *connect.BidiStream[v1.SubsystemMessage, v1.GatewayMessage]) error
}

// NewGatewayServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGatewayServiceHandler(svc GatewayServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gatewayServiceMethods := v1.File_flowwatch_v1_gateway_service_proto.Services().ByName("GatewayService").Methods()
	gatewayServiceRegisterHandler := connect.NewBidiStreamHandler(
		GatewayServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(gatewayServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	return "/flowwatch.v1.GatewayService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GatewayServiceRegisterProcedure:
			gatewayServiceRegisterHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGatewayServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGatewayServiceHandler struct{}

func (UnimplementedGatewayServiceHandler) Register(context.Context, *connect.BidiStream[v1.SubsystemMessage, v1.GatewayMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("flowwatch.v1.GatewayService.Register is not implemented"))
}
