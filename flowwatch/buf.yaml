version: v2
modules:
  - path: proto
    name: buf.build/luno/flowwatch
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
  except:
    - FIELD_NOT_REQUIRED  # we intentionally use optional fields
breaking:
  use:
    - FILE
